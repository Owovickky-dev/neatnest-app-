import 'package:flutter/material.dart';

class AddressHolder extends StatelessWidget {
  const AddressHolder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
