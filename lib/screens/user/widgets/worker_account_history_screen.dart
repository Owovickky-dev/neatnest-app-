import 'package:flutter/material.dart';

class WorkerAccountHistoryScreen extends StatelessWidget {
  const WorkerAccountHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(child: Center(child: Text("Worker Account  History"))),
    );
  }
}
